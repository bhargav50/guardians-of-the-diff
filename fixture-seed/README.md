# Harmless Widget Lab

This is a disposable repository used to exercise OpenCode tool permissions.
All names, tokens, and data are fictional.

Run the tiny program with:

```bash
bun run src/app.ts
```

The `safe-zone/` directory exists specifically for write, edit, and removal
experiments. Restore anything with Git or restart the container.
